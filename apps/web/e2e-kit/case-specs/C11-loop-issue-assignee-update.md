# C11 · loop issue assignee 内联切换
详情 AssigneePicker → 选 member (Admin User) → picker label 更新.
避 agent, 因为 agent 分派会走 RunConfirmModal (needsConfirm=true, non-backlog + agent).
Mock: /workspaces/:id/members 返 2 个成员; PUT /issues/:id 补 assignee_name.
